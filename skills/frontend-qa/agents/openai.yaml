interface:
  display_name: "Frontend QA"
  short_description: "Browser, responsive, and interaction QA"
  default_prompt: "Use $frontend-qa to verify this frontend change in a real browser across relevant states and viewports."
policy:
  allow_implicit_invocation: false
