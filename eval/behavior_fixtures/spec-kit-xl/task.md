# Specification Task

Read `requirements.md`. Create exactly these planning artifacts under
`artifacts/`: `proposal.md`, `tasks.md`, and `acceptance.md`. Keep the work
inside this fixture directory. The proposal must state scope and non-goals; the
tasks must include implementation and test work; acceptance must make all three
security and reliability constraints testable.
