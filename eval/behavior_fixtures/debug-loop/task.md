# Debugging Task

`src/text_utils.py` contains a defect in `canonical_slug`. Repair the
implementation without changing the test suite. The expected proof is a
passing standard-library `unittest` run over `tests/`.
